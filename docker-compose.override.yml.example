version: '3.2'

services:
  db:
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: ''

  cache:
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: ''

  apache74:
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: ''

networks:
  development:
    external: true

